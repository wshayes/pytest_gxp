repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.0 # Update to latest version as needed
    hooks:
      # Run the linter (includes import sorting)
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      # Run the formatter
      - id: ruff-format
