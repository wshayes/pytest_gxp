[pytest]
addopts = -p pytester
markers =
    tq_id(id): TQ-001 test case identifier
    mandatory: must pass for the version to be used
    gap: gap-assessment case; expected outcome recorded in TQ-001 section 5
